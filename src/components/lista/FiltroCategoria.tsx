export function FiltroCategoria({ categorias, activa, onChange }: { categorias: string[]; activa: string; onChange: (c: string) => void }) { return <div className="flex gap-2 overflow-auto pb-2" role="list">{["Todas", ...categorias].map((c) => <button key={c} onClick={() => onChange(c)} className={`touch rounded-full border px-4 text-sm font-bold transition ${activa === c ? "border-tinta bg-tinta text-white" : "border-tinta/10 bg-white/60 text-tinta/65 hover:bg-white"}`}>{c}</button>)}</div>; }
