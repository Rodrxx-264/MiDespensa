export function FiltroCategoria({ categorias, activa, onChange }: { categorias: string[]; activa: string; onChange: (c: string) => void }) { return <div className="flex gap-2 overflow-auto pb-2" role="list">{["Todas", ...categorias].map((c) => <button key={c} onClick={() => onChange(c)} className={`touch rounded-full px-4 text-sm font-bold ${activa === c ? "bg-bosque text-white" : "bg-white text-bosque"}`}>{c}</button>)}</div>; }
