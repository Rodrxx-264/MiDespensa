import type { Producto } from "@/types";
export function ProductosFrecuentes({ productos }: { productos: Producto[] }) { const top = Object.entries(productos.reduce<Record<string, number>>((a, p) => { a[p.nombre] = (a[p.nombre] ?? 0) + 1; return a; }, {})).sort((a,b)=>b[1]-a[1]).slice(0,10); return <section className="panel rounded-[28px] p-5"><h2 className="text-xl font-black tracking-[-.04em]">Productos más comprados</h2><ol className="mt-4 space-y-2">{top.map(([n,c]) => <li key={n} className="flex justify-between rounded-2xl bg-white/55 p-3"><span>{n}</span><b>{c}</b></li>)}</ol></section>; }
